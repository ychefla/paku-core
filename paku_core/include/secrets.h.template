// secrets.h.template - Copy this file to secrets.h and fill in your credentials
// DO NOT commit secrets.h to version control!
#pragma once

// Wi-Fi Networks (in order of priority)
static const char* WIFI_SSIDS[] = { "your-wifi-ssid-1", "your-wifi-ssid-2", "your-wifi-ssid-3" };
static const char* WIFI_PASSWORDS[] = { "your-wifi-password-1", "your-wifi-password-2", "your-wifi-password-3" };
static const size_t WIFI_COUNT = sizeof(WIFI_SSIDS) / sizeof(WIFI_SSIDS[0]);

// MQTT Broker
#define MQTT_SERVER                   "your-mqtt-server-hostname"
#define MQTT_PORT                     1883

// paku-iot HTTP Endpoint (optional - for direct HTTP transport)
// Set PAKU_IOT_ENABLED to 1 to enable HTTP transport to paku-iot
#define PAKU_IOT_ENABLED              0
#define PAKU_IOT_HOST                 "iot.paku.example.com"
#define PAKU_IOT_PORT                 443
#define PAKU_IOT_API_KEY              "your-api-key"
#define PAKU_IOT_USE_TLS              true

// RuuviTag Configuration (optional - for pre-registering known tags)
// Uncomment and configure to pre-register RuuviTags with known locations.
// If not configured, tags will be auto-discovered with location names like "tag_0", "tag_1", etc.
// 
// IMPORTANT: 
// - RUUVI_TAG_COUNT must match the actual number of entries in both arrays!
// - MAC addresses must be in UPPERCASE with colons (e.g., "AA:BB:CC:DD:EE:FF")
// - Location names will be used as device_id in MQTT topics: paku/sensors/{location}/data
// - Use short, lowercase names without spaces (e.g., "cabin", "kitchen", "bedroom")
//
// #define RUUVI_TAG_COUNT 4
// static const char* RUUVI_TAG_MACS[] = {
//     "AA:BB:CC:DD:EE:01",  // Replace with actual MAC from RuuviTag
//     "AA:BB:CC:DD:EE:02",
//     "AA:BB:CC:DD:EE:03",
//     "AA:BB:CC:DD:EE:04"
// };
// static const char* RUUVI_TAG_LOCATIONS[] = {
//     "cabin",      // Friendly name for first tag
//     "kitchen",
//     "lounge",
//     "dryer"
// };

// MoKo Sensor Configuration (optional - for pre-registering known sensors)
// Uncomment and configure to pre-register MoKo sensors (H2/H3/H4 series) with known locations.
// If not configured, sensors will be auto-discovered with location names like "auto_AABB", etc.
// 
// IMPORTANT: 
// - MOKO_SENSOR_COUNT must match the actual number of entries in both arrays!
// - MAC addresses must be in UPPERCASE with colons (e.g., "AA:BB:CC:DD:EE:FF")
// - Location names will be used as device_id in MQTT topics: paku/sensors/moko_{location}/data
// - Use short, lowercase names without spaces (e.g., "garage", "basement", "outdoor")
// - MoKo sensors typically broadcast with names like "MK_H3", "MK_H4", etc.
//
// #define MOKO_SENSOR_COUNT 2
// static const char* MOKO_SENSOR_MACS[] = {
//     "AA:BB:CC:DD:EE:11",  // Replace with actual MAC from MoKo sensor
//     "AA:BB:CC:DD:EE:12"
// };
// static const char* MOKO_SENSOR_LOCATIONS[] = {
//     "garage",     // Friendly name for first sensor
//     "outdoor"
// };

// OTA Update Configuration (optional - for firmware updates)
// The device will listen for OTA commands on MQTT topic:
//   paku/devices/{device_id}/cmd/ota
// 
// Command format (JSON):
// {
//   "url": "https://your-server.com/firmware/paku-core-v1.2.0.bin",
//   "checksum": "sha256-hex-string",
//   "version": "1.2.0"
// }
//
// The firmware binary should be accessible via HTTP/HTTPS.
// Checksum is optional but highly recommended for security.
// After successful update, the device will automatically reboot.
//
// Example OTA server URL (replace with your actual server):
// #define OTA_SERVER_URL "https://ota.paku.example.com/firmware"
//
// For signature verification (future enhancement):
// #define OTA_PUBLIC_KEY "-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
